def project(name):
    return #TODO this function will create the folder and will create also a file call package.json will bin the folder and a file call main.guic and a sub folder call packpage were it's all the packpage download

def package(name, url):
    return #TODO this function will add a package it will be a dict like this {"name" : "url_github"} it will download the zip of the project and will save it in the su folder packpage